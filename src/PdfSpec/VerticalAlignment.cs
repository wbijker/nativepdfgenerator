namespace PdfSpec;

/// <summary>
/// Vertical positioning of a component inside taller available space —
/// inside a row cell, a frame slot, or a container with explicit
/// height.
/// </summary>
public enum VerticalAlignment { Top, Middle, Bottom }
