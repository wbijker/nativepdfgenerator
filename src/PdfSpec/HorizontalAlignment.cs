namespace PdfSpec;

/// <summary>
/// Horizontal positioning of a component inside wider available space —
/// inside a stack column whose content advertises a narrower natural
/// width, a row cell with horizontal slack, or a container with explicit
/// width.
/// </summary>
public enum HorizontalAlignment { Left, Center, Right }
