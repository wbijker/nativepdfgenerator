namespace CSharpPdf.Layout;

/// <summary>Length units that can be passed to <c>Fixed(size, unit)</c>.</summary>
public enum Unit
{
    /// <summary>PDF default user-space unit (one point = 1/72 inch).</summary>
    Px,
}
