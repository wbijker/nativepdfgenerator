namespace CSharpPdf.Layout;

/// <summary>How a component is positioned horizontally within wider available space.</summary>
public enum HorizontalAlignment { Left, Center, Right }

/// <summary>How a component is positioned vertically within taller available space (e.g. a row cell).</summary>
public enum VerticalAlignment { Top, Middle, Bottom }
